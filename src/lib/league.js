// The competition — and its prize pool — only activates once this many
// patriots have filed at least one prediction. Until then the game is fully
// playable, but prizes show as "pending" (see useActivePlayers).
export const MIN_PLAYERS = 50;
